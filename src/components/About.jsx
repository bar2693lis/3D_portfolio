import React from "react";
import { Tilt } from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { fadeIn, textVariant } from "../utils/motion";
import { SectionWrapper } from "../hoc";

const ServiceCard = ({ index, title, icon }) => {
  return (
    <Tilt className="xs:w-[250px] w-full">
      <motion.div
        variants={fadeIn("right", "spring", index * 0.5, 0.75)}
        className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
      >
        <div
          options={{ max: 45, scale: 1, speed: 450 }}
          className="bg-tertiary rounded-[20px] py-24 px-5 mix-h-[280px] flex justify-evenly items-center flex-col"
        >
          <img src={icon} alt={title} className="w-16 h-16 object-contain" />
          <h3 className=" text-white text-[20x] font-bold text-center">
            {title}
          </h3>
        </div>
      </motion.div>
    </Tilt>
  );
};

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-[#4191b3] text-[17px] max-w-3xl leading-[30px]"
      >
        Greetings! I'm a passionate software sorcerer armed with a Bachelor's
        degree in Computer Science. With a magical mix of TypeScript,
        JavaScript, and Python spells in my toolkit, I wield expertise in
        enchanting frameworks like React, Node.js, Next.js, Three.js, and
        more—think of me as a tech wizard with a diverse spellbook! Not just a
        run-of-the-mill developer, I'm a quick learner and a committed team
        player who thrives on conjuring up efficient, scalable, and
        user-friendly solutions to tackle real-world problems. Let's team up to
        sprinkle some coding magic and bring your wildest ideas to life!
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
